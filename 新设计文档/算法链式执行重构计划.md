# 算法链式执行重构计划

**文档版本**: v1.0
**创建日期**: 2025-11-19
**状态**: 待实施
**策略**: 0新类方案 - 不考虑向后兼容，直接全面切换

---

## 📌 目录

- [1. 背景与目标](#1-背景与目标)
- [2. 当前问题分析](#2-当前问题分析)
- [3. 设计原则](#3-设计原则)
- [4. 技术方案](#4-技术方案)
- [5. 实施计划](#5-实施计划)
- [6. 测试验证](#6-测试验证)
- [7. 风险评估](#7-风险评估)
- [8. 时间线估算](#8-时间线估算)

---

## 1. 背景与目标

### 1.1 背景

当前算法执行架构存在以下问题：

1. **结果交付的"最后一公里"不清晰**
   - AlgorithmManager 直接操作 CurveManager（越权）
   - AlgorithmCoordinator 将结果存入 Context（无人读取）
   - MainController 收到通知后无实际处理（责任缺失）

2. **不支持算法链式执行**
   - 信号只携带算法名（信息不足）
   - 并发执行会覆盖结果（存储缺陷）
   - 无法自动触发下一步算法（缺少编排）

3. **设计预留功能未激活**
   - `AlgorithmDescriptor` 的 `prerequisites/produces` 字段从未使用
   - `OutputKeys::latestResult()` 存储机制有覆盖问题
   - Context 中的结果无人读取，成为"死数据"

### 1.2 目标

**核心目标**: 在不新增类的前提下，实现算法链式执行能力，同时修复现有架构问题。

**具体目标**:
- ✅ 支持并发执行多个算法，结果互不覆盖
- ✅ 支持历史结果查询（每条曲线保留最近 N 次执行）
- ✅ 支持线性工作流（自动链式执行多个算法）
- ✅ 强类型信号机制（携带完整结果信息）
- ✅ 职责清晰（Coordinator 编排，Manager 落地，Controller 决策）

---

## 2. 当前问题分析

### 2.1 信号机制问题

**现状**:
```cpp
// AlgorithmCoordinator
emit algorithmSucceeded(const QString& algorithmName);  // 只有算法名
```

**问题**:
- ❌ 无法区分并发任务（缺少 taskId）
- ❌ 不知道是对哪条曲线执行的（缺少 parentCurveId）
- ❌ 不包含结果数据（缺少 AlgorithmResult）
- ❌ UI 必须从 Context 读取结果（耦合 OutputKeys 内部实现）

### 2.2 存储机制问题

**现状**:
```cpp
// AlgorithmCoordinator::saveResultToContext
m_context->setValue(
    OutputKeys::latestResult(algorithmName),  // 键名: result/{algorithm}/latest
    QVariant::fromValue(result)
);
```

**问题**:
- ❌ 覆盖式存储（同一算法多次执行会丢失历史）
- ❌ 并发执行结果混乱（最后一次覆盖前面的）
- ❌ 无法区分不同曲线的结果（键中缺少 curveId）

### 2.3 职责分散问题

**现状**:
```
AlgorithmManager::handleAlgorithmResult()
  → AlgorithmManager::addCurveWithHistory()  // 直接操作 CurveManager

AlgorithmCoordinator::onAsyncAlgorithmFinished()
  → saveResultToContext()  // 缓存结果（但无人读取）
  → emit algorithmSucceeded(algorithmName)  // 弱通知

MainController::onCoordinatorAlgorithmSucceeded()
  → cleanupProgressDialog()  // 只做清理，不处理结果
```

**问题**:
- ❌ Manager 越权（应用层直接操作领域模型）
- ❌ Coordinator 职责不明（编排还是缓存？）
- ❌ Controller 责任缺失（无法主动感知结果）

---

## 3. 设计原则

### 3.1 核心原则

1. **0新类原则**
   - 不引入 Repository、ResultHandler、WorkflowManager 等新类
   - 仅在现有类内增加小范围方法与信号
   - 保持架构简洁，避免过度设计

2. **单一职责原则**
   - **AlgorithmManager**: 统一结果落地（曲线→CurveManager，标注→信号）
   - **AlgorithmCoordinator**: 流程编排与结果缓存
   - **MainController**: 决策与 UI 交互

3. **信息完备原则**
   - 信号必须携带完整信息（taskId, algorithmName, parentCurveId, result）
   - 避免强制下游访问 Context 内部实现

4. **不覆盖原则**
   - 按 taskId 存储主数据（不可覆盖）
   - 按 curveId 维护索引（快速查询）

### 3.2 架构约束

- ✅ **Coordinator 只编排，不落地**: 结果保存到 Context，发出完成信号
- ✅ **Manager 统一落地**: 曲线添加到 CurveManager，标注/区域发专用信号
- ✅ **UI 订阅模型事件**: 通过 CurveManager 和专用信号更新，不访问 Context
- ✅ **Context 仅用于缓存和查询**: 支持历史查询和算法链依赖检查

---

## 4. 技术方案

### 4.1 键空间设计

**混合索引方案**（taskId 主键 + curveId 索引）:

```
主存储（不可覆盖，便于历史/并发）：
  result/{algorithm}/byTask/{taskId} → AlgorithmResult

曲线索引（快速定位最新与历史）：
  result/{algorithm}/byCurve/{parentCurveId}/latestTaskId → taskId
  result/{algorithm}/byCurve/{parentCurveId}/historyTaskIds → [taskId1, taskId2, ...]
```

**优势**:
- ✅ 不会覆盖历史（taskId 主键保证唯一性）
- ✅ 并发安全（每次执行独立存储）
- ✅ 快速查询（latestTaskId 指针 O(1) 定位）
- ✅ 可控深度（historyTaskIds 支持 LRU 裁剪）

### 4.2 信号机制重构

**删除**:
```cpp
// 旧信号（信息不足）
void algorithmSucceeded(const QString& algorithmName);
```

**新增**:
```cpp
// 强类型完成信号（唯一的结果通知信号）
void algorithmCompleted(const QString& taskId,
                       const QString& algorithmName,
                       const QString& parentCurveId,
                       const AlgorithmResult& result);
```

**统一同步和异步**:
- 同步执行：生成 taskId = `{algorithmName}-{timestamp}`
- 异步执行：使用 Worker 返回的 taskId
- 两者都发射同一个 `algorithmCompleted` 信号

### 4.3 轻量级工作流

**在 AlgorithmCoordinator 内部实现**（不新建类）:

```cpp
// 类内小结构
struct PendingWorkflow {
    QString id;                         // 工作流实例ID
    QStringList steps;                  // 算法步骤列表
    int currentStepIndex = 0;           // 当前执行到第几步
    QStringList inputCurveIds;          // 初始输入曲线
    QHash<int, QStringList> stepOutputs; // 每步的输出曲线ID
    WorkflowStatus status;
    QString errorMessage;
};

// 新增方法
QString runWorkflow(const QStringList& steps, const QStringList& curveIds);
void cancelWorkflow(const QString& workflowId);

// 新增信号
void workflowCompleted(const QString& workflowId, const QStringList& outputCurveIds);
void workflowFailed(const QString& workflowId, const QString& errorMessage);
```

**特性**:
- ✅ 仅支持线性流程（不支持 DAG）
- ✅ 单实例执行（不支持并发工作流）
- ✅ 自动推进（监听 algorithmCompleted 信号）
- ✅ 支持取消（短路后续步骤）

### 4.4 API 轮廓

#### OutputKeys（新增函数）

```cpp
namespace OutputKeys {
    // 删除旧函数
    // QString latestResult(const QString& algorithmName);
    // QString resultType(const QString& algorithmName);

    // 新增函数
    QString byTask(const QString& algorithm, const QString& taskId);
    QString latestTaskId(const QString& algorithm, const QString& curveId);
    QString historyTaskIds(const QString& algorithm, const QString& curveId);
}
```

#### AlgorithmContext（新增方法）

```cpp
class AlgorithmContext {
public:
    // 新增：结果专用 API
    void saveResult(const QString& taskId,
                   const QString& algorithm,
                   const QString& parentCurveId,
                   const AlgorithmResult& result);

    std::optional<AlgorithmResult> latestResult(const QString& algorithm,
                                                const QString& curveId) const;

    QVector<AlgorithmResult> historyResults(const QString& algorithm,
                                           const QString& curveId,
                                           int limit = 10) const;

    void setHistoryDepth(int depth);
    int historyDepth() const;

private:
    int m_historyDepth = 10;  // 新增成员
};
```

#### AlgorithmCoordinator（调整）

```cpp
class AlgorithmCoordinator {
public:
    // 删除方法
    // void saveResultToContext(const QString&, const AlgorithmResult&);

    // 新增：工作流
    enum class WorkflowStatus { Idle, Running, Completed, Failed, Cancelled };
    QString runWorkflow(const QStringList& steps, const QStringList& curveIds);
    void cancelWorkflow(const QString& workflowId);

signals:
    // 删除信号
    // void algorithmSucceeded(const QString& algorithmName);

    // 新增信号
    void algorithmCompleted(const QString& taskId,
                           const QString& algorithmName,
                           const QString& parentCurveId,
                           const AlgorithmResult& result);

    void workflowCompleted(const QString& workflowId, const QStringList& outputCurveIds);
    void workflowFailed(const QString& workflowId, const QString& errorMessage);

private slots:
    // 重命名（避免混淆）
    void onSyncAlgorithmResultReady(const QString&, const AlgorithmResult&);

private:
    struct PendingWorkflow { /*...*/ };
    std::optional<PendingWorkflow> m_currentWorkflow;

    void advanceWorkflow(const QString& taskId,
                        const QString& algorithmName,
                        const AlgorithmResult& result);
};
```

---

## 5. 实施计划

### 阶段 1: 基础设施层（无依赖）

#### 1.1 OutputKeys 键生成工具增强

**文件**: `Analysis/src/domain/algorithm/algorithm_result.h`

**改动清单**:
- [ ] 删除 `latestResult(algorithmName)` 函数
- [ ] 删除 `resultType(algorithmName)` 函数
- [ ] 新增 `byTask(algorithm, taskId)` 函数
- [ ] 新增 `latestTaskId(algorithm, curveId)` 函数
- [ ] 新增 `historyTaskIds(algorithm, curveId)` 函数

**验收标准**:
```cpp
// 测试键生成
QString key1 = OutputKeys::byTask("differentiation", "task-001");
QCOMPARE(key1, "result/differentiation/byTask/task-001");

QString key2 = OutputKeys::latestTaskId("differentiation", "curve-123");
QCOMPARE(key2, "result/differentiation/byCurve/curve-123/latestTaskId");
```

---

#### 1.2 AlgorithmContext 结果存取封装

**文件**:
- `Analysis/src/application/algorithm/algorithm_context.h`
- `Analysis/src/application/algorithm/algorithm_context.cpp`

**改动清单**:
- [ ] 新增 `m_historyDepth` 成员变量（默认10）
- [ ] 新增 `saveResult()` 方法（实现 LRU 裁剪）
- [ ] 新增 `latestResult()` 方法（读取最新指针 → 展开 taskId）
- [ ] 新增 `historyResults()` 方法（读取历史队列 → 展开列表）
- [ ] 新增 `setHistoryDepth()/historyDepth()` 方法

**验收标准**:
```cpp
// 测试并发保存
context->saveResult("task-001", "differentiation", "curve-123", result1);
context->saveResult("task-002", "differentiation", "curve-123", result2);
context->saveResult("task-003", "differentiation", "curve-123", result3);

auto latest = context->latestResult("differentiation", "curve-123");
QVERIFY(latest.has_value());
QCOMPARE(latest->algorithmKey(), "differentiation");
// 验证是 task-003 的结果

auto history = context->historyResults("differentiation", "curve-123");
QCOMPARE(history.size(), 3);
// 验证按时间倒序：[result3, result2, result1]
```

**LRU 裁剪测试**:
```cpp
context->setHistoryDepth(3);

// 执行 5 次
for (int i = 0; i < 5; ++i) {
    QString taskId = QString("task-%1").arg(i);
    context->saveResult(taskId, "differentiation", "curve-123", result);
}

auto history = context->historyResults("differentiation", "curve-123");
QCOMPARE(history.size(), 3);  // 只保留最近 3 次
```

---

### 阶段 2: 协调层

#### 2.1 AlgorithmCoordinator 信号机制重构

**文件**:
- `Analysis/src/application/algorithm/algorithm_coordinator.h`
- `Analysis/src/application/algorithm/algorithm_coordinator.cpp`

**改动清单**:

**删除**:
- [ ] 删除 `saveResultToContext()` 方法
- [ ] 删除 `onAlgorithmResultReady()` 槽函数
- [ ] 删除 `algorithmSucceeded(QString)` 信号

**新增**:
- [ ] 新增 `algorithmCompleted(taskId, algorithmName, parentCurveId, result)` 信号
- [ ] 新增 `WorkflowStatus` 枚举
- [ ] 新增 `PendingWorkflow` 结构体（类内）
- [ ] 新增 `runWorkflow()` 方法
- [ ] 新增 `cancelWorkflow()` 方法
- [ ] 新增 `advanceWorkflow()` 私有方法
- [ ] 新增 `workflowCompleted()` 信号
- [ ] 新增 `workflowFailed()` 信号

**修改**:
- [ ] 重命名 `onAlgorithmResultReady()` → `onSyncAlgorithmResultReady()`
- [ ] 修改 `onSyncAlgorithmResultReady()` 实现：
  - 生成 taskId（算法名 + 时间戳）
  - 调用 `context->saveResult()`
  - 发射 `algorithmCompleted()`
  - 调用 `advanceWorkflow()`
- [ ] 修改 `onAsyncAlgorithmFinished()` 实现：
  - 调用 `context->saveResult()`
  - 删除 `emit algorithmSucceeded()`
  - 发射 `algorithmCompleted()`
  - 调用 `advanceWorkflow()`

**验收标准**:
```cpp
// 测试单次执行信号
QSignalSpy spy(coordinator, &AlgorithmCoordinator::algorithmCompleted);
coordinator->executeAlgorithm("differentiation", "curve-001");
QCOMPARE(spy.count(), 1);

QList<QVariant> args = spy.takeFirst();
QCOMPARE(args.at(1).toString(), "differentiation");  // algorithmName
QCOMPARE(args.at(2).toString(), "curve-001");        // parentCurveId
```

---

#### 2.2 工作流实现

**改动清单**:
- [ ] 实现 `runWorkflow()` 方法
  - 生成工作流 ID
  - 初始化 `PendingWorkflow` 状态
  - 执行第一步算法
- [ ] 实现 `advanceWorkflow()` 方法
  - 检查是否有活动工作流
  - 验证算法名匹配当前步骤
  - 记录当前步骤输出
  - 推进到下一步或完成工作流
- [ ] 实现 `cancelWorkflow()` 方法
  - 标记状态为 Cancelled
  - 清理工作流状态

**验收标准**:
```cpp
// 测试线性工作流
QSignalSpy completedSpy(coordinator, &AlgorithmCoordinator::workflowCompleted);

QString wfId = coordinator->runWorkflow(
    {"movingAverage", "differentiation"},
    {"curve-001"}
);

// 等待工作流完成
QVERIFY(completedSpy.wait(5000));
QCOMPARE(completedSpy.count(), 1);

QList<QVariant> args = completedSpy.takeFirst();
QCOMPARE(args.at(0).toString(), wfId);

// 验证生成了两条曲线（平滑 + 微分）
QStringList outputs = args.at(1).toStringList();
QCOMPARE(outputs.size(), 1);  // 最终输出是微分曲线
```

---

### 阶段 3: 算法增强

#### 3.1 AlgorithmDescriptor 依赖声明

**文件**: 所有算法实现（5个）

**改动清单**:
- [ ] **DifferentiationAlgorithm**:
  - `prerequisites = {"activeCurve"}`
  - `produces = {"outputCurve"}`
- [ ] **IntegrationAlgorithm**:
  - `prerequisites = {"activeCurve"}`
  - `produces = {"outputCurve"}`
- [ ] **MovingAverageFilterAlgorithm**:
  - `prerequisites = {"activeCurve"}`
  - `produces = {"outputCurve"}`
- [ ] **BaselineCorrectionAlgorithm**:
  - `prerequisites = {"activeCurve", "selectedPoints"}`
  - `produces = {"outputCurve"}`
- [ ] **TemperatureExtrapolationAlgorithm**:
  - `prerequisites = {"activeCurve", "baselineCurve"}`
  - `produces = {"markers", "scalar"}`

**验收标准**:
```cpp
auto desc = algorithm->descriptor();
QVERIFY(!desc.prerequisites.isEmpty());
QVERIFY(!desc.produces.isEmpty());
```

---

#### 3.2 依赖校验实现

**文件**: `algorithm_coordinator.cpp`

**改动清单**:
- [ ] 新增 `checkPrerequisites(algorithmName)` 私有方法
- [ ] 在 `executeAlgorithm()` 前调用校验
- [ ] 缺失依赖时发出 `showMessage` 信号

**验收标准**:
```cpp
// 测试依赖校验
curveManager->clearAll();  // 清空曲线

bool canRun = coordinator->checkPrerequisites("differentiation");
QVERIFY(!canRun);  // 缺少 activeCurve

curveManager->addCurve(curve);
curveManager->setActiveCurve("curve-001");

canRun = coordinator->checkPrerequisites("differentiation");
QVERIFY(canRun);  // 依赖满足
```

---

### 阶段 4: UI 层调整

#### 4.1 MainController 信号连接

**文件**:
- `Analysis/src/ui/controller/main_controller.h`
- `Analysis/src/ui/controller/main_controller.cpp`

**改动清单**:

**删除**:
- [ ] 删除 `onCoordinatorAlgorithmSucceeded()` 槽函数
- [ ] 删除 `algorithmSucceeded` 信号的 connect 语句

**新增**:
- [ ] 新增 `onAlgorithmCompleted()` 槽函数（4个参数）
- [ ] 新增 `onWorkflowCompleted()` 槽函数
- [ ] 新增 `onWorkflowFailed()` 槽函数
- [ ] 连接 `algorithmCompleted` 信号
- [ ] 连接 `workflowCompleted` 信号
- [ ] 连接 `workflowFailed` 信号

**实现**:
```cpp
void MainController::onAlgorithmCompleted(
    const QString& taskId,
    const QString& algorithmName,
    const QString& parentCurveId,
    const AlgorithmResult& result)
{
    Q_ASSERT(m_initialized);

    qInfo() << "算法执行完成:" << algorithmName
            << "taskId:" << taskId
            << "源曲线:" << parentCurveId
            << "结果类型:" << static_cast<int>(result.type());

    cleanupProgressDialog();
    m_currentTaskId.clear();
    m_currentAlgorithmName.clear();

    // 可选：自动化工作流决策
    // if (algorithmName == "differentiation") {
    //     QString dtgCurveId = result.outputCurveIds().first();
    //     m_algorithmCoordinator->executeAlgorithm("peakDetection", dtgCurveId);
    // }
}
```

**验收标准**:
```cpp
// 测试信号连接
QSignalSpy spy(controller, SIGNAL(/* 内部信号 */));
coordinator->executeAlgorithm("differentiation", "curve-001");
// 验证 controller 的槽函数被调用
```

---

### 阶段 5: 清理旧代码

#### 5.1 清理 Context 中的旧数据

**改动清单**:
- [ ] 删除所有使用 `OutputKeys::latestResult(algorithmName)` 的代码
- [ ] 删除所有使用 `OutputKeys::resultType(algorithmName)` 的代码
- [ ] 搜索 `get.*latestResult` 确认无遗留调用

**验收**:
```bash
# 搜索确认
grep -r "latestResult" --include="*.cpp" --include="*.h" Analysis/src/
# 应该只在 OutputKeys 定义和 AlgorithmContext 实现中出现
```

---

## 6. 测试验证

### 6.1 单元测试

#### 测试 1: 并发执行（无覆盖）

```cpp
void TestAlgorithmContext::testConcurrentExecution()
{
    AlgorithmContext context;

    AlgorithmResult result1 = AlgorithmResult::success("differentiation", "curve-001", ResultType::Curve);
    AlgorithmResult result2 = AlgorithmResult::success("differentiation", "curve-001", ResultType::Curve);
    AlgorithmResult result3 = AlgorithmResult::success("differentiation", "curve-001", ResultType::Curve);

    // 并发保存
    context.saveResult("task-001", "differentiation", "curve-001", result1);
    context.saveResult("task-002", "differentiation", "curve-001", result2);
    context.saveResult("task-003", "differentiation", "curve-001", result3);

    // 验证最新结果
    auto latest = context.latestResult("differentiation", "curve-001");
    QVERIFY(latest.has_value());

    // 验证历史结果
    auto history = context.historyResults("differentiation", "curve-001");
    QCOMPARE(history.size(), 3);

    // 验证主存储（byTask）
    auto task1 = context.get<AlgorithmResult>(OutputKeys::byTask("differentiation", "task-001"));
    QVERIFY(task1.has_value());
}
```

#### 测试 2: LRU 裁剪

```cpp
void TestAlgorithmContext::testHistoryLRU()
{
    AlgorithmContext context;
    context.setHistoryDepth(3);  // 只保留 3 次

    // 执行 5 次
    for (int i = 0; i < 5; ++i) {
        QString taskId = QString("task-%1").arg(i);
        AlgorithmResult result = AlgorithmResult::success("differentiation", "curve-001", ResultType::Curve);
        context.saveResult(taskId, "differentiation", "curve-001", result);
    }

    // 验证历史队列只有 3 个
    auto history = context.historyResults("differentiation", "curve-001");
    QCOMPARE(history.size(), 3);

    // 验证是最近的 3 个（task-4, task-3, task-2）
    QString latestKey = OutputKeys::latestTaskId("differentiation", "curve-001");
    QString latestTaskId = context.get<QString>(latestKey).value();
    QCOMPARE(latestTaskId, "task-4");
}
```

#### 测试 3: 工作流执行

```cpp
void TestAlgorithmCoordinator::testWorkflowExecution()
{
    QSignalSpy completedSpy(coordinator, &AlgorithmCoordinator::workflowCompleted);
    QSignalSpy failedSpy(coordinator, &AlgorithmCoordinator::workflowFailed);

    // 准备测试曲线
    ThermalCurve curve = createTestCurve();
    curveManager->addCurve(curve);

    // 执行工作流
    QString wfId = coordinator->runWorkflow(
        {"movingAverage", "differentiation"},
        {curve.id()}
    );

    QVERIFY(!wfId.isEmpty());

    // 等待完成（最多 5 秒）
    QVERIFY(completedSpy.wait(5000));
    QCOMPARE(completedSpy.count(), 1);
    QCOMPARE(failedSpy.count(), 0);

    // 验证输出
    QList<QVariant> args = completedSpy.takeFirst();
    QCOMPARE(args.at(0).toString(), wfId);

    QStringList outputs = args.at(1).toStringList();
    QCOMPARE(outputs.size(), 1);  // 最终输出曲线
}
```

#### 测试 4: 依赖校验

```cpp
void TestAlgorithmCoordinator::testPrerequisites()
{
    curveManager->clearAll();

    // 无活动曲线时
    bool canRun = coordinator->checkPrerequisites("differentiation");
    QVERIFY(!canRun);

    // 添加曲线并设为活动
    ThermalCurve curve = createTestCurve();
    curveManager->addCurve(curve);
    curveManager->setActiveCurve(curve.id());

    // 再次检查
    canRun = coordinator->checkPrerequisites("differentiation");
    QVERIFY(canRun);
}
```

### 6.2 集成测试

#### 测试 5: 端到端工作流

```cpp
void TestIntegration::testEndToEndWorkflow()
{
    // 1. 导入数据
    ThermalCurve curve = importTestData("test_tga.txt");

    // 2. 执行工作流
    QString wfId = coordinator->runWorkflow(
        {"movingAverage", "differentiation", "peakDetection"},
        {curve.id()}
    );

    // 3. 等待完成
    QSignalSpy spy(coordinator, &AlgorithmCoordinator::workflowCompleted);
    QVERIFY(spy.wait(10000));

    // 4. 验证结果
    QStringList outputs = spy.at(0).at(1).toStringList();
    QVERIFY(!outputs.isEmpty());

    // 5. 验证历史记录
    auto smoothHistory = context->historyResults("movingAverage", curve.id());
    QCOMPARE(smoothHistory.size(), 1);

    auto diffHistory = context->historyResults("differentiation", outputs.first());
    QCOMPARE(diffHistory.size(), 1);
}
```

---

## 7. 风险评估

### 7.1 高风险项

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| **信号连接遗漏** | 中 | 高 | 编译时搜索所有 `algorithmSucceeded` 确认删除干净 |
| **Context 键名冲突** | 低 | 中 | 使用 OutputKeys 统一生成，避免硬编码字符串 |
| **工作流状态混乱** | 中 | 中 | 单实例限制，添加状态日志，单元测试覆盖 |
| **LRU 裁剪丢失重要数据** | 低 | 低 | 配置可调（默认10），关键任务不裁剪 |

### 7.2 中风险项

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| **旧代码回归** | 中 | 中 | 全面搜索 `latestResult`/`algorithmSucceeded` |
| **性能问题（频繁存储）** | 低 | 低 | Context 已有优化，必要时批量写入 |
| **工作流取消不彻底** | 中 | 低 | 取消时清理所有状态，添加集成测试 |

### 7.3 低风险项

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| **依赖校验误报** | 低 | 低 | 完善校验逻辑，添加单元测试 |
| **历史查询性能** | 低 | 低 | 限制查询深度（默认10），延迟加载 |

---

## 8. 时间线估算

### 8.1 工作量估算

| 阶段 | 任务 | 预计工时 | 优先级 |
|------|------|---------|--------|
| **阶段1** | OutputKeys 增强 | 0.5h | P0 |
| **阶段1** | AlgorithmContext 增强 | 2h | P0 |
| **阶段2** | AlgorithmCoordinator 信号重构 | 3h | P0 |
| **阶段2** | 工作流实现 | 4h | P1 |
| **阶段3** | 算法依赖声明 | 1h | P2 |
| **阶段3** | 依赖校验实现 | 1.5h | P2 |
| **阶段4** | MainController 调整 | 1h | P0 |
| **阶段5** | 清理旧代码 | 1h | P1 |
| **测试** | 单元测试编写 | 4h | P0 |
| **测试** | 集成测试验证 | 2h | P1 |
| **总计** | - | **20h** | - |

### 8.2 里程碑

| 里程碑 | 交付物 | 截止日期 |
|--------|--------|---------|
| **M1: 基础设施完成** | OutputKeys + AlgorithmContext 实现并通过单元测试 | Day 1 |
| **M2: 信号机制完成** | AlgorithmCoordinator 重构完成，发射新信号 | Day 2 |
| **M3: 工作流完成** | 线性工作流可用，通过集成测试 | Day 3 |
| **M4: 全面集成** | UI 层调整完成，旧代码清理干净 | Day 4 |
| **M5: 发布就绪** | 所有测试通过，文档更新完成 | Day 5 |

### 8.3 建议开发顺序

**第 1 天**（核心基础）:
1. OutputKeys 增强（0.5h）
2. AlgorithmContext 增强（2h）
3. 单元测试编写（AlgorithmContext）（1.5h）
4. 代码审查与调整（0.5h）

**第 2 天**（协调层重构）:
1. AlgorithmCoordinator 信号删除与新增（2h）
2. 同步/异步槽函数改造（1h）
3. MainController 信号连接调整（1h）
4. 单元测试（信号发射）（1h）

**第 3 天**（工作流实现）:
1. PendingWorkflow 结构设计（0.5h）
2. runWorkflow() 实现（1.5h）
3. advanceWorkflow() 实现（1.5h）
4. cancelWorkflow() 实现（0.5h）
5. 工作流单元测试（2h）

**第 4 天**（算法增强与集成）:
1. 5个算法 prerequisites/produces 补充（1h）
2. 依赖校验实现（1.5h）
3. 清理旧代码（1h）
4. 集成测试编写（2h）

**第 5 天**（测试与优化）:
1. 端到端测试（1h）
2. 性能测试与优化（1h）
3. 代码审查与 bug 修复（2h）
4. 文档更新（1h）

---

## 9. 验收标准

### 9.1 功能验收

- [ ] **并发执行**: 同时执行 3 次微分，结果互不覆盖
- [ ] **历史查询**: 可以查询某条曲线某算法的最近 10 次执行
- [ ] **LRU 裁剪**: 执行 15 次后只保留最近 10 次
- [ ] **工作流执行**: 成功执行 2-3 步线性工作流
- [ ] **工作流取消**: 可以在执行过程中取消
- [ ] **依赖校验**: 缺少前置依赖时提示错误
- [ ] **信号机制**: 所有算法完成时发射 `algorithmCompleted` 信号

### 9.2 代码质量验收

- [ ] **无编译警告**: 编译时无 warning
- [ ] **无旧代码残留**: 搜索确认无 `algorithmSucceeded`/旧 `latestResult`
- [ ] **无硬编码键名**: 所有 Context 访问使用 OutputKeys
- [ ] **单元测试覆盖**: 核心方法测试覆盖率 > 80%
- [ ] **集成测试通过**: 端到端工作流测试通过

### 9.3 性能验收

- [ ] **存储性能**: 1000 次执行的保存时间 < 100ms
- [ ] **查询性能**: 历史查询（10条）时间 < 10ms
- [ ] **工作流延迟**: 3步工作流的总开销 < 50ms（不含算法执行）

---

## 10. 后续扩展方向

### 10.1 短期（Phase 2）

- [ ] **工作流 DAG 支持**: 允许分支和聚合节点
- [ ] **并发工作流**: 支持同时执行多个工作流实例
- [ ] **工作流持久化**: 保存工作流定义到文件/数据库
- [ ] **自动触发规则**: 在 AlgorithmDescriptor 中定义 `autoTrigger`

### 10.2 中期（Phase 3）

- [ ] **工作流可视化编辑器**: 拖拽式工作流设计
- [ ] **条件分支**: 根据算法结果选择不同路径
- [ ] **循环执行**: 支持批量处理多条曲线
- [ ] **错误恢复**: 失败步骤的重试机制

### 10.3 长期（Phase 4）

- [ ] **脚本化工作流**: 支持 Lua/Python 脚本定义流程
- [ ] **分布式执行**: 工作流步骤分布到多台机器
- [ ] **增量计算**: 缓存中间结果，避免重复计算
- [ ] **版本管理**: 工作流定义的版本控制

---

## 附录

### A. 关键类图

```
AlgorithmContext (基础设施)
  ├─ saveResult(taskId, algorithm, curveId, result)
  ├─ latestResult(algorithm, curveId)
  └─ historyResults(algorithm, curveId, limit)

AlgorithmCoordinator (协调层)
  ├─ algorithmCompleted(taskId, algorithm, curveId, result) [信号]
  ├─ workflowCompleted(workflowId, outputs) [信号]
  ├─ runWorkflow(steps, curveIds)
  └─ advanceWorkflow(taskId, algorithm, result) [私有]

MainController (UI层)
  ├─ onAlgorithmCompleted(...) [槽函数]
  ├─ onWorkflowCompleted(...) [槽函数]
  └─ onWorkflowFailed(...) [槽函数]
```

### B. 数据流图

```
用户点击"执行微分"
  ↓
MainController::onAlgorithmRequested()
  ↓
AlgorithmCoordinator::executeAlgorithm()
  ↓
AlgorithmManager::executeAsync()
  ↓
AlgorithmWorker::run() (工作线程)
  ↓
AlgorithmManager::onWorkerFinished()
  ├─ handleAlgorithmResult() → 落地到 CurveManager
  └─ emit algorithmFinished(taskId, algorithmName, result, elapsedMs)
      ↓
AlgorithmCoordinator::onAsyncAlgorithmFinished()
  ├─ context->saveResult(taskId, ...) → 缓存结果
  ├─ emit algorithmCompleted(taskId, ...) → 通知 UI
  └─ advanceWorkflow(...) → 推进工作流（如有）
      ↓
MainController::onAlgorithmCompleted()
  └─ cleanupProgressDialog() + 自定义逻辑
```

### C. 键空间示例

```
# 执行 3 次微分算法

主存储（永久保留）：
  result/differentiation/byTask/task-001 → AlgorithmResult (timestamp: 10:00)
  result/differentiation/byTask/task-002 → AlgorithmResult (timestamp: 10:05)
  result/differentiation/byTask/task-003 → AlgorithmResult (timestamp: 10:10)

曲线索引（快速查询）：
  result/differentiation/byCurve/curve-001/latestTaskId → "task-003"
  result/differentiation/byCurve/curve-001/historyTaskIds → ["task-003", "task-002", "task-001"]
```

---

**文档结束**

**审批流程**:
- [ ] 技术负责人审批
- [ ] 架构师审批
- [ ] 开始实施
