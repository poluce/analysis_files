# 未使用函数整理

# 1. 概述

本文件整理了项目中扫描到的所有 **未被调用的函数（Unused Functions）**，并按照模块进行归类。

------

# 2. 未使用函数分类整理

------

## 2.1 AlgorithmContext / AlgorithmManager / AlgorithmCoordinator

### **AlgorithmContext**

- `value(const QString& key, const QVariant& defaultValue) const`
- `values(const QString& prefix) const`
- `clear()`
- `value(const QString& key)`（重载）
- `values(QString prefix)`（重载）

### **AlgorithmCoordinator**

- `handleParameterSubmission(const QString&, const QVariantMap&)`

### **AlgorithmManager**

- `executeWithContext(const QString& name, AlgorithmContext* context)`
- `handleParameterSubmission(const QString&, const QVariantMap&)`

**备注：**
 以上全部属于旧“上下文读取 + 参数提交”流程，现已被 AlgorithmDescriptor + AlgorithmTask 体系替代。

------

## 2.2 HistoryManager / AlgorithmResult

（旧历史记录体系）

### **HistoryManager**

- `setHistoryLimit(int limit)`
- `undoCount() const`
- `redoCount() const`
- `historyLimit() const`

### **AlgorithmResult**

- `historyResult(const QString&, int)`
- `algorithmKey() const`
- `timestamp() const`
- `addCurve(const ThermalCurve&)`
- `primaryCurve() const`
- `setMarkers(const QList<QPointF>&)`
- `setRegions(const QList<QPolygonF>&)`
- `regions() const`
- `setPeakPosition(const QPointF&)`
- `peakPosition() const`
- `signalType() const`

------

## 2.3 ThermalCurve / BaselineCorrectionAlgorithm

### **ThermalCurve**

- `plotStyle() const`
- `setPlotStyle(PlotStyle)`
- `setName(const QString&)`
- `resetToRaw()`
- `getRawData() const`

### **BaselineCorrectionAlgorithm**

- `findNearestPoint(const QVector<ThermalDataPoint>&, double temperature) const`

------

## 2.4 ChartView（图表交互工具——旧体系）

### **质量损失工具（MassLossTools）**

- `addMassLossTool(...)`
- `removeMassLossTool(...)`
- `clearAllMassLossTools()`

### **峰面积工具（PeakAreaTools）**

- `addPeakAreaTool(...)`
- `removePeakAreaTool(...)`
- `clearAllPeakAreaTools()`

### **标记线 / 十字线工具**

- `cancelAlgorithmInteraction()`
- `verticalCrosshairEnabled() const`
- `horizontalCrosshairEnabled() const`
- `clearAllMarkers()`
- `removeCurveMarkers(const QString& curveId)`

### **状态/交互相关（旧体系）**

- `interactionState() const`
- `activeAlgorithm() const`
- `selectedPoints() const`
- `selectedPointsCurveId() const`

------

## 2.5 CurveViewController / ThermalChart

（控制器与图表逻辑）

### **CurveViewController**

- `updateAllCurves()`

### **ThermalChart**

- `point1() const`
- `point2() const`
- `areaPolygon() const`
- `toggleXAxisMode()`
- `setCustomTitles()`
- `clearCustomTitles()`
- `rescaleYAxisForXRange(...)`
- `primaryAxisY() const`
- `secondaryAxisY() const`

### **ThermalChartView**

- `hitThreshold() const`
- `hitThreshold()`（重载）
- `thermalChart() const`
- `interactionMode() const`

------

## 2.6 PeakAreaTool / TrapezoidMeasureTool

（旧量测工具）

### **PeakAreaTool**

- `point1() const`
- `point2() const`
- `areaPolygon() const`

### **TrapezoidMeasureTool**

- `updatePosition()`
- `point1() const`
- `point2() const`

