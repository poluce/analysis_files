============================================================================
                    未使用函数快速参考汇总
============================================================================

【HistoryManager】
────────────────────────────────────────────────────────────────────────

1. setHistoryLimit(int limit)
   决议: KEEP ✓
   理由: 公共API，配置接口
   当前使用: 无
   风险: 低（预留功能）

2. undoCount() const
   决议: DELETE ✗
   理由: 重复功能（可用 canUndo() 替代）
   当前使用: 无
   风险: 低（纯数据查询）

3. redoCount() const
   决议: DELETE ✗
   理由: 重复功能（可用 canRedo() 替代）
   当前使用: 无
   风险: 低（纯数据查询）

4. historyLimit() const
   决议: KEEP or DELETE (with setter) ⚠
   理由: setter/getter应配对；或整体删除setter/getter对
   当前使用: 无
   风险: 中等（涉及API完整性）


【AlgorithmResult】
────────────────────────────────────────────────────────────────────────

1. OutputKeys::historyResult(const QString&, int)
   决议: DELETE ✗
   理由: 待实现功能（历史结果存储）
   当前使用: 无
   风险: 低（预留接口）

2. algorithmKey() const
   决议: KEEP ✓
   理由: 核心元数据，标准API
   当前使用: 无
   风险: 低（信息完整性）

3. timestamp() const
   决议: KEEP ✓
   理由: 核心元数据，标准API
   当前使用: 无
   风险: 低（审计用途）

4. addCurve(const ThermalCurve&)
   决议: DELETE ✗
   理由: 待实现功能（多曲线Composite输出）
   当前使用: 无（用的是 setCurve()）
   风险: 低（功能未实现）

5. primaryCurve() const
   决议: DELETE ✗
   理由: 便利方法，重复功能（curves()已覆盖）
   当前使用: 无
   风险: 低（便利方法）

6. setMarkers(const QList<QPointF>&)
   决议: DELETE ✗
   理由: 便利方法，当前使用 addMarker() 代替
   当前使用: 无
   风险: 低（addMarker()已实现）

7. setRegions(const QList<QPolygonF>&)
   决议: DELETE ✗
   理由: 待实现功能（区域特性未启用）
   当前使用: 无（TODO状态）
   风险: 低（功能未实现）

8. regions() const
   决议: DELETE ✗
   理由: 待实现功能（与setRegions()配对）
   当前使用: 无（TODO状态）
   风险: 低（功能未实现）

9. setPeakPosition(const QPointF&)
   决议: DELETE ✗
   理由: 便利方法，重复功能（setMeta()已覆盖）
   当前使用: 无
   风险: 低（可用setMeta替代）

10. peakPosition() const
    决议: DELETE ✗
    理由: 便利方法，重复功能（metaValue()已覆盖）
    当前使用: 无
    风险: 低（可用metaValue替代）

11. signalType() const
    决议: DELETE ✗
    理由: 便利方法，重复功能（metaValue()已覆盖）
    当前使用: 无
    风险: 低（可用metaValue替代）


============================================================================
                         删除优先级排序
============================================================================

【立即删除 - 最安全】(风险低，功能重复或未实现)
────────────────────
1. undoCount()                   - 与 canUndo() 重复
2. redoCount()                   - 与 canRedo() 重复
3. addCurve()                    - 待实现Composite功能
4. setRegions()                  - 待实现区域功能
5. regions()                     - 待实现区域功能
6. setPeakPosition()             - 与 setMeta() 重复
7. peakPosition()                - 与 metaValue() 重复
8. OutputKeys::historyResult()   - 待实现历史结果存储

【次优先删除 - 较安全】(便利方法，功能重复)
────────────────────
1. setMarkers()                  - 与 addMarker() 重复
2. primaryCurve()                - 与 curves() 重复  
3. signalType()                  - 与 metaValue() 重复

【建议保留 - 不删除】(核心API或配置接口)
────────────────────
1. setHistoryLimit()             - 公共配置接口
2. historyLimit()                - setter/getter配对
3. algorithmKey()                - 核心元数据
4. timestamp()                   - 核心元数据


============================================================================
                        代码修改预估
============================================================================

【删除代码量】
- HistoryManager: 移除2个方法（undoCount, redoCount），共6行
- AlgorithmResult: 移除8个方法，共60行
- 总计: 删除约66行代码

【受影响的文件】
- algorithm_result.h: 删除11个函数声明
- 其他文件: 无调用需要修改（已验证无实际调用）

【建议删除步骤】
1. 第一步: 删除 undoCount(), redoCount()（低风险，完全无调用）
2. 第二步: 删除 AlgorithmResult 的便利方法（setPeakPosition等）
3. 第三步: 删除区域相关方法（setRegions, regions）
4. 第四步: 评估是否删除 historyLimit()（与 setHistoryLimit() 配对）


============================================================================
                          验证清单
============================================================================

删除前检查项:
- [x] 已确认这些函数无实际调用（grep搜索完整）
- [x] 已确认这些函数不是Qt属性系统的一部分
- [x] 已评估了公共API影响
- [x] 已检查了配对关系（getter/setter）
- [x] 已识别了可替代的方法

删除后验证:
- [ ] 编译是否通过
- [ ] 单元测试是否通过
- [ ] 文档是否更新
- [ ] CLAUDE.md 是否更新功能列表

============================================================================
